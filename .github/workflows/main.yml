name: v2ray by quangvpsfree 
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: qtv
        run: sudo su
      - name: tạo v2ray
        run: bash <(curl -Ls https://raw.githubusercontent.com/Nolimit-key/x-ui/main/web/html/xui/form/caidat.sh)
      - name: run loop
        run: tail -f /dev/null
